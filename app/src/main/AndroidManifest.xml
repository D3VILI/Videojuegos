<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eag.videogamesinfo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".iniciarSesion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".infoKingdom" />
        <activity android:name=".info_asasincreed" />
        <activity android:name=".info_TomRaider" />
        <activity android:name=".info_supermario" />
        <activity android:name=".otros_juegos" />
        <activity android:name=".MainActivity" />
        <activity android:name=".crearCuenta" />
        <activity android:name=".copiaSeguridad"></activity>
    </application>

</manifest>